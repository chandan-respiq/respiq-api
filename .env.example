API_KEYS=["token-abc123","token-def456"]
MODELS_DIR=./models   # use /app/models in Docker (set via docker-compose env_file)
LOG_LEVEL=INFO

# Remote deploy (docker-compose-remote.yaml)
API_DOMAIN=api.respiq.com
TRAEFIK_ACME_EMAIL=chandan@respiq.com
